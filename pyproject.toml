[project]
name = "pyghtcast"
version = "0.1.0"
description = "A library to interact with Lightcast API"
authors = [
    { name = "Ammar N", email = "ananjiani@dallascollege.edu" }
]
license = { text = "GPL-3.0" }
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "pandas>=2.2.2",
    "requests>=2.32.3",
    "click>=8.1.0",
]

[project.scripts]
pyghtcast = "pyghtcast.cli:cli"

[project.optional-dependencies]
dev = [
    "mypy>=1.0.0",
    "ruff>=0.3.0",
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "types-click>=7.1.0",
    "pandas-stubs>=2.0.0",
    "types-requests>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 120
target-version = "py312"  # Using py312 until ruff is updated

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
    "C901", # mccabe cyclomatic complexity
]
ignore = [
    "E501",  # line too long, handled by formatter
    "B008",  # do not perform function calls in argument defaults
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_any_unimported = false
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

# pandas/requests/click/pytest stubs are listed as dev/runtime deps but the nix
# mypy pre-commit hook (and `uv run --with mypy`) may run without them; ignore
# missing imports so all environments typecheck cleanly (stubs used if present).
[[tool.mypy.overrides]]
module = ["pandas", "pandas.*", "requests", "requests.*", "click", "click.*", "pytest", "pytest.*"]
ignore_missing_imports = true

# Tests are not shipped library code; relax strictness for the pre-commit
# hook which passes test paths to mypy directly (resolved as bare module names
# like `test_cli`, hence the explicit names rather than a glob).
[[tool.mypy.overrides]]
module = ["tests.*", "test_cli", "test_base", "test_sanity", "test_connection"]
disallow_untyped_defs = false
check_untyped_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
addopts = "--cov=pyghtcast --cov-report=term-missing"

[tool.coverage.run]
source = ["pyghtcast"]
omit = ["*/tests/*", "*/examples/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
    "pass",
    "except ImportError:",
]
